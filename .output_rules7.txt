Searched codebase for "Audit complete. This is another excellent, highly dense Spoke. 
Here are the rulings on your 5 audit calls:
Bulldozer pattern: Edit to Invariant. `TEXT_STORE` being the *only* bulldozed store is a hard safety limit. We do not want the AI to ever consider bulldozing user data stores. 
`SCHEMA_VERSION`: Keep. Relying on the cache key composition for clean invalidation is elegant.
Hot-swap fallback: Edit. You correctly identified the Literature mode flaw. Modify the example to fall back to `memoryCache[0].id` instead of hardcoding `1001001`. That safely handles both Bibles and Books.
Store-name constants: Keep as Invariant. Hardcoded strings lead to silent failures.
Cross-refs: Keep.
Please proceed with the following actions:
Apply the two edits and write `.rules/data-persistence.md` to disk. 
Create `.rules/_index.md` containing the one-line registry of all 6 spokes. 
Once the registry is written, the architecture is fully deployed. We are ready to return to the roadmap. We will begin R-1: Build `jit/vault.js`. Please review the `jit-feature.md` and `data-persistence.md` spokes, and provide a diff to implement `vault.js` and wire it into `app.js` or wherever necessary.", 22 results. Files: c:\nbs\abible\.output_rules3.txt, c:\nbs\abible\.output_rules3.txt, c:\nbs\abible\.output_rules3.txt, c:\nbs\abible\.output_rules3.txt, c:\nbs\abible\.output_rules3.txt, c:\nbs\abible\.output_rules3.txt, c:\nbs\abible\.output_rules4.txt, c:\nbs\abible\.output_rules4.txt, c:\nbs\abible\.output_rules4.txt, c:\nbs\abible\.output_rules4.txt, c:\nbs\abible\.output_rules4.txt, c:\nbs\abible\.output_rules4.txt, c:\nbs\abible\.output_rules4.txt, c:\nbs\abible\.output_rules5.txt, c:\nbs\abible\.output_rules5.txt, c:\nbs\abible\.rules\jit-feature.md, c:\nbs\abible\config.js, c:\nbs\abible\db.js, c:\nbs\abible\project_brief.md, c:\nbs\abible\project_brief.md, c:\nbs\abible\project_brief.md, c:\nbs\abible\project_brief.md
Thinking: Now I'm applying those edits—moving the Bulldozer pattern to INVARIANTS and fixing the hot-swap fallback to use `memoryCache[0].id`—then creating the _index.md file and building out vault.js with the getKey and setKey methods that handle key persistence and announce the last four digits.
The remaining vault.js methods need to handle clearing keys, displaying redacted versions, and ensuring the database is ready before any operation, while being careful never to log or expose raw keys in errors. I'm noticing the roadmap separates building vault.js itself from wiring it into the app—R-1 is just the vault module, while R-3 and R-4 handle integration into the Options menu and orchestrator respectively. Even though the user mentioned wiring it in, the roadmap structure suggests I should focus on completing vault.js as a standalone module first.
I'll build vault.js and surface the wiring scope as a follow-up step, giving the user the choice to expand. I need to check what dependencies vault requires—the database readiness check, the API_KEYS_STORE, and ui.js for audio feedback—and match the style of existing jit modules.
Running Remove-Item -Path &quot;c:\nbs\abible\.rules\data-persistence.md&quot; -Force -ErrorAct.... Remove-Item -Path "c:\nbs\abible\.rules\data-persistence.md" -Force -ErrorAction SilentlyContinue; New-Item -ItemType Directory -Path "c:\nbs\abible\.rules" -Force | Out-Null
Read jit
Read errors.js, lines 1 to 40
Created data-persistence.md
Created _index.md
Architecture deployed.
.rules/data-persistence.md (/c:/nbs/abible/.rules/data-persistence.md)
,
.rules/_index.md (/c:/nbs/abible/.rules/_index.md)
,
.rules/jit-feature.md (/c:/nbs/abible/.rules/jit-feature.md)
,
project_brief.md (/c:/nbs/abible/project_brief.md)
all live.
R-1 — Build jit/vault.js
Block-and-ask first (per §0 directive on conflict with §6/§12).
Your prompt asked me to "implement vault.js and wire it into
app.js (/c:/nbs/abible/app.js)
or wherever necessary." That conflates two roadmap items:
Item Scope
R-1 Build jit/vault.js (pure leaf module).
R-3 Wire vault into Options menu UX (Save/Replace/Clear key entries).
R-4 Wire orchestrator → vault (pre-call key fetch).
The roadmap deliberately separated these because R-3 needs UX decisions still in §13 OQ-7 (confirmation prompt vs. immediate overwrite for "Replace key"), and R-4 needs the planCache (R-2) in place first.
My recommendation: ship R-1 alone as a pure leaf module — no UI wiring, no orchestrator wiring. Verify with a quick console-side smoke test, then proceed to R-2. R-3 and R-4 land cleanly afterward.
If you concur, here is the diff for R-1 only. No
app.js (/c:/nbs/abible/app.js)
or
keyboard.js (/c:/nbs/abible/keyboard.js)
changes. New file only.
```
--- /dev/null
+++ b/jit/vault.js
@@ -0,0 +1,138 @@
+/**
+ * jit/vault.js
+ * Local API-key vault for the JIT Study Plan feature.
+ *
+ * Contract (locked in .rules/jit-feature.md):
+ *   getKey(provider)            -> Promise<string | null>
+ *   setKey(provider, rawKey)    -> Promise<void>   // announces redacted last-4
+ *   clearKey(provider)          -> Promise<void>
+ *   redactedDisplay(provider)   -> Promise<string> // "•••• 7g4Q" or "Not configured"
+ *
+ * INVARIANTS:
+ *   - whenDbReady() awaited at the top of every tx.
+ *   - rawKey NEVER logged, NEVER included in thrown error messages,
+ *     NEVER returned in any stringified form except the live string itself.
+ *   - On any tx error, reject with a generic Error — no key payload attached.
+ *   - Plaintext storage today; encryption envelope reserved (`keyEnvelope` field).
+ */
+
+import { db, whenDbReady } from '../db.js';
+import { API_KEYS_STORE } from '../config.js';
+import { speak } from '../ui.js';
+
+// --- Internal helpers -------------------------------------------------------
+
+function readRecord(provider) {
+    return new Promise((resolve, reject) => {
+        const tx = db.transaction([API_KEYS_STORE], 'readonly');
+        const req = tx.objectStore(API_KEYS_STORE).get(provider);
+        req.onsuccess = () => resolve(req.result || null);
+        req.onerror = () => reject(new Error('Vault read failed.'));
+    });
+}
+
+function writeRecord(record) {
+    return new Promise((resolve, reject) => {
+        const tx = db.transaction([API_KEYS_STORE], 'readwrite');
+        tx.objectStore(API_KEYS_STORE).put(record);
+        tx.oncomplete = () => resolve();
+        tx.onerror = () => reject(new Error('Vault write failed.'));
+    });
+}
+
+function deleteRecord(provider) {
+    return new Promise((resolve, reject) => {
+        const tx = db.transaction([API_KEYS_STORE], 'readwrite');
+        tx.objectStore(API_KEYS_STORE).delete(provider);
+        tx.oncomplete = () => resolve();
+        tx.onerror = () => reject(new Error('Vault clear failed.'));
+    });
+}
+
+/**
+ * Last-4 redaction. NEVER call with anything except a verified raw key
+ * about to be displayed; never log the result with surrounding context
+ * that could narrow brute-force scope.
+ */
+function redact(rawKey) {
+    if (!rawKey || typeof rawKey !== 'string' || rawKey.length < 4) {
+        return '••••';
+    }
+    return `•••• ${rawKey.slice(-4)}`;
+}
+
+// --- Public API -------------------------------------------------------------
+
+/**
+ * Returns the raw key string for a provider, or null if none configured.
+ * Caller (orchestrator) is expected to branch on null and throw AuthError.
+ */
+export async function getKey(provider) {
+    if (!provider || typeof provider !== 'string') {
+        throw new Error('Vault: provider name required.');
+    }
+    await whenDbReady();
+    const record = await readRecord(provider);
+    return record?.rawKey ?? null;
+}
+
+/**
+ * Persists the key for a provider. Announces the redacted last-4 to the
+ * screen reader so the user can confirm capture without ever hearing the
+ * full secret.
+ */
+export async function setKey(provider, rawKey) {
+    if (!provider || typeof provider !== 'string') {
+        throw new Error('Vault: provider name required.');
+    }
+    if (!rawKey || typeof rawKey !== 'string' || rawKey.trim().length === 0) {
+        throw new Error('Vault: empty key rejected.');
+    }
+    await whenDbReady();
+    const record = {
+        provider,                  // keyPath
+        rawKey: rawKey.trim(),     // plaintext today
+        keyEnvelope: null,         // reserved for future encryption-at-rest
+        savedAt: Date.now(),
+    };
+    await writeRecord(record);
+    // Announce confirmation with redaction. Never speak the full key.
+    speak(`Key saved. Ending in ${rawKey.trim().slice(-4)}.`);
+}
+
+/**
+ * Removes the stored key for a provider. Idempotent.
+ */
+export async function clearKey(provider) {
+    if (!provider || typeof provider !== 'string') {
+        throw new Error('Vault: provider name required.');
+    }
+    await whenDbReady();
+    await deleteRecord(provider);
+    speak('Key cleared.');
+}
+
+/**
+ * Returns a TTS-safe redacted string for menu display.
+ * Never includes the full key. Falls through to "Not configured."
+ */
+export async function redactedDisplay(provider) {
+    if (!provider || typeof provider !== 'string') {
+        return 'Not configured';
+    }
+    await whenDbReady();
+    const record = await readRecord(provider);
+    if (!record || !record.rawKey) return 'Not configured';
+    return redact(record.rawKey);
+}
+
+/**
+ * Convenience boolean for orchestrator pre-call gating.
+ */
+export async function hasKey(provider) {
+    const key = await getKey(provider);
+    return typeof key === 'string' && key.length > 0;
+}
```
Notes on the diff:
**138 lines.** Pure leaf module. Zero call-site changes elsewhere.
**Imports only:**
db.js (/c:/nbs/abible/db.js)
(
db (c:\nbs\abible\db.js:9)
,
whenDbReady (c:\nbs\abible\db.js:136)
),
config.js (/c:/nbs/abible/config.js)
(
API_KEYS_STORE (c:\nbs\abible\config.js:7)
),
ui.js (/c:/nbs/abible/ui.js)
(
speak (c:\nbs\abible\db.js:2)
). No imports from
keyboard.js (/c:/nbs/abible/keyboard.js)
or
app.js (/c:/nbs/abible/app.js)
— vault stays UI-agnostic.
**Three internal helpers** (`readRecord`, `writeRecord`, `deleteRecord`) wrap raw IndexedDB transactions. Each rejects with a *generic*
Error (c:\BCI\VSCode-win32-x64-1.116.0\560a9dba96\resources\app\extensions\node_modules\typescript\lib\lib.es2022.error.d.ts:23)
— never includes the key payload, satisfying §5 invariant b.
**Five public exports:** `getKey`, `setKey`, `clearKey`, `redactedDisplay`, plus a `hasKey` convenience for the orchestrator.
**Encryption-ready slot:** `keyEnvelope: null` field reserved on every record. When R-6 (Cycle 3 encryption envelope) lands, `keyEnvelope` becomes the ciphertext and `rawKey` becomes a getter that decrypts.
**
speak() (c:\nbs\abible\db.js:2)
calls** are deliberate UX confirmations (not log statements) — they speak only the redacted last-4 or status, never the full key.
**No catches around
whenDbReady() (c:\nbs\abible\db.js:136)
** — if the DB never becomes ready, the promise hangs forever, which is correct: we cannot operate without persistence.
Audit calls before I write the file:
**
speak() (c:\nbs\abible\db.js:2)
from inside vault.js** — ergonomic but couples the leaf module to
ui.js (/c:/nbs/abible/ui.js)
. Alternative: return a status string and let the caller speak it. Lean: keep, since the redaction is the security-critical step and centralizing it here prevents a caller from accidentally announcing the wrong portion.
2. `hasKey` export — minor convenience. Could be inlined at the orchestrator. Drop if you want a stricter five-method surface.
3. Empty-key rejection in setKey — I throw a generic
Error (c:\BCI\VSCode-win32-x64-1.116.0\560a9dba96\resources\app\extensions\node_modules\typescript\lib\lib.es2022.error.d.ts:23)
. Should this be a
ValidationError (c:\nbs\abible\jit\errors.js:49)
from
errors.js (/c:/nbs/abible/jit/errors.js)
instead, so the Options-menu wrapper can route it through the same
userMessage (c:\nbs\abible\jit\errors.js:9)
/
recoverable (c:\nbs\abible\jit\errors.js:24)
machinery? Lean: yes, but that would create a circular concern (vault importing
errors.js (/c:/nbs/abible/jit/errors.js)
for one throw). Inclined to leave as plain Error and have the caller wrap.
4. Provider parameter — I made every method require an explicit provider name. The current scope is Gemini-only. Should I default to &#39;gemini&#39; to reduce caller boilerplate, or keep explicit for D-007 / future multi-provider hygiene?
5. **No wiring to
app.js (/c:/nbs/abible/app.js)
** — confirming you accept R-1-only scope and we defer the Options-menu UX to R-3.
Mark each Keep / Edit / Drop. On approval I write jit/vault.js. After that we move to R-2 (jit/planCache.js).