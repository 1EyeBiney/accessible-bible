 
JIT Curriculum Integration: Project State & Roadmap
Project: The Accessible Bible (Offline-first, keyboard-driven, screen-reader optimized)
Feature: Dynamic Just-In-Time (JIT) Study Plans
🛑 Phase 0: Milestones Achieved (The Sandbox)
• 
BYOK Security Proven: Local Node.js sandbox utilizing .env to read API keys.
• 
Model Locked: gemini-2.5-flash selected.
• 
Schema Fences Secured: Strict JSON output forced via responseSchema.
• 
Happy Path Verified: Successfully generated structured, filtered study plans.
✅ Update Cycle 1: Sandbox Reconnaissance (COMPLETED)
Goal: Map the exact shape of Gemini's error and safety envelopes.
• 
Findings:
• 
Hard blocks trigger explicit 'SAFETY' reason codes.
• 
Contextual safety holds up under strict schema constraints.
• 
Schema absurdities/hallucinations break JSON.parse(), requiring a wrapper.
• 
Network limits (503/429) require standard API retry/handling.
🚀 Update Cycle 2: Architecture & The Semantic Safety Catch (CURRENT)
Goal: Build the frontend pipeline using Opus's recommended accessibility-first sequence.
• 
Task 2.1 (The Error Hierarchy): Create errors.js. Build a flat hierarchy (e.g., SafetyError, NetworkError, ParsingError) extending a base StudyPlanError. Crucial: Enforce mandatory userMessage and recoverable fields for native TTS integration.
• 
Task 2.2 (The Semantic Catch): Build a local keyword/regex Sensitive-Topic Detector to run before the API call, and add a mandatory disclaimer field to the JSON schema to prevent "valid JSON but harmful content."
• 
Task 2.3 (The Provider): Build GeminiProvider.js — handles ONLY the wire protocol, auth, and schema coercion.
• 
Task 2.4 (The Validator): Build PlanValidator.js — handles ONLY domain logic (manifest existence, fuzzy text matching).
• 
Task 2.5 (The Orchestrator): Build generateStudyPlan() — the single "choke point" wrapper that executes the Provider and Validator, catching all raw exceptions so they never leak to the UI.
• 
Task 2.6 (The Vault & Cache): Build the IndexedDB API_KEYS_STORE (with Options UX) and STUDYPLANS_STORE.
• 
Task 2.7 (Engine Integration): Wire the orchestrator into the keyboard.js auto-jump sequence and TTS engine.
 
📋 The Re-Onboarding Prompt for Opus
Use this prompt if VS Code restarts or context clears to instantly sync Opus to our exact position:
Plaintext
Download codeCopy code
Role: Senior Systems Architect

Context Update: We are actively developing the JIT Study Plan feature for the "Accessible Bible" (an offline-first, screen-reader-optimized web app). 

We have successfully completed our local Node.js sandbox stress testing. Based on your previous architectural advice, we are now executing "Update Cycle 2: Architecture & The Semantic Safety Catch."

We have adopted your specific recommendations:
1. We are using a flat Error hierarchy (StudyPlanError base) requiring 'userMessage' and 'recoverable' fields for TTS routing.
2. We are separating the GeminiProvider (wire protocol) from the PlanValidator (domain/fuzzy matching).
3. We are wrapping everything in a single `generateStudyPlan` orchestrator choke-point.
4. We are implementing a local Sensitive-Topic regex detector to run before API calls to catch pastorally inappropriate prompts that bypass standard AI safety filters.

We are currently working on Step X of this cycle [Insert current step here, e.g., "drafting the errors.js file"]. Could you review our implementation for this step?
 