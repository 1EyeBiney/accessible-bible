### v62.3 Added Books
### v62.2 — Vertical Readout Patch
- **Consistency Fix:** Updated the Arrow Up (Personal Note readout) listener to respect Book Mode. It now correctly announces "Notes are disabled in Book mode" instead of attempting a database lookup.
### v62.1 — Literature UX Routing
- **Feature Fencing:** Disabled the Note editor (`M`) and Expert Commentary (`Y`) triggers while in Book Mode, replacing them with explicit audio feedback ("Disabled in Book mode") to prevent user confusion.
- **Menu Override:** Bypassed the Verse Menu (`U`) entirely when in Book Mode, converting the keystroke into an instant "Copy Paragraph" macro.
- **Global Settings:** Retained global access to the Options menu (`O`) across all modes to ensure users can always export/import data.
### v62.0 — Contextual Bookmarks & Metadata Jumps
- **Contextual Filtering:** Refactored the IndexedDB bookmark loader (`db.js`) to filter saved bookmarks against the active memory cache. This prevents cross-contamination between Bibles and literature, effectively establishing a 10-bookmark quota per individual document.
- **Smart Bookmark Audio:** Updated the `MapsBookmarks` logic (`app.js`) to check the active read mode. In literature mode, jumping to a bookmark now announces the bookmark number, current chapter, and exact percentage completed.
- **Smart Search Audio:** Applied the contextual metadata logic to the Word Search carousel in `keyboard.js`. Search matches in books now announce chapter and percentage, remaining completely silent regarding verses.
### v61.2 — Chronos Anti-Freeze & Hybrid Scrubbing
- **Main Thread Optimization:** Replaced the expensive Regex string-splitting logic in the Chronos time-jump loop with a standard typographic word-length estimation (`text.length / 6`), preventing NVDA and browser freezes during massive calculations.
- **Hybrid Jump Array:** Upgraded the Auto Jump selector to a hybrid array (`10s`, `30s`, `1m`, `5m`, `15m`, `1%`, `5%`, `10%`). Time limits are capped at 15 minutes to minimize TTS drift, while percentage jumps offer perfectly precise absolute navigation for large-scale movement.
- **Safety Net Math:** Added a `Math.max(1, ...)` floor to all jump calculations to ensure that even 1% jumps in extremely short books will always move the index by at least one paragraph.
### v61.1 — UX Polish & State Persistence Fixes
- **Mode Persistence:** The `activeReadMode` ('bible' vs 'book') is now saved to `localStorage` during fetches, preventing the engine from reverting to Bible mode (and speaking verse numbers) after a hard refresh of a literature file.
- **Contextual Tab:** Updated the 'Where Am I?' (`Tab` key) logic in literature mode to announce the Chapter number and percentage completed, rather than the raw paragraph count.
- **Chronos Input Fix:** Added a strict modifier check to the Personal Note (`ArrowUp`) key listener to prevent it from swallowing `Shift + ArrowUp` inputs, restoring the ability to cycle the Chronos Auto Jump amounts.
### v61.0 — Chronos Part 2: Time-Based Scrubbing & UX Polish
- **Time-Jump Engine:** Implemented `Shift + Left/Right` to skip forward/backward based on time, using a dynamic Math-to-Words-Per-Minute conversion tied to the active Auto Play speech rate.
- **Auto-Jump Interface:** Added `Shift + Up/Down` to cycle time jump amounts (10s, 30s, 1m, 5m, 15m, 1h) with progressive disclosure TTS instructions.
- **Contextual 'Where Am I':** Updated the `Tab` key math. In Literature mode, it now calculates and announces the exact percentage completed (e.g., "Paragraph X of Y. 45 percent completed") instead of just standard references.
- **Verse Menu Relocation:** Re-mapped the Verse Menu from `ArrowDown` to the `U` key to prevent vertical navigation collisions and group verse actions (Y, U, M) together.
- **Stopwatch Polish:** Injected the missing `playAutoPlayUI('stop')` chime into `autoplay.js` so the engine audibly signs off when time limits expire.
### v60.0 — Chronos Part 1: Auto Play Stopwatch Limits
- **Menu Expansion:** Added "Minutes" as a fourth unit option in the Auto Play configuration menu alongside Verses, Chapters, and Books.
- **Stopwatch Architecture:** Implemented a non-destructive timer sequence in `autoplay.js` that records the start time (`Date.now()`) when playback begins.
- **Time-Boundary Enforcement:** The playback loop now checks the elapsed time against the user's selected minute threshold after every paragraph, gracefully stopping the engine and respecting the existing post-focus settings when the time limit is reached.
### v59.0 — Literature Mode & TTS Intelligence
- **Labeling Pivot:** Restored logic to switch between "Verse" (Bible) and "Paragraph" (Book) labels in the visual display.
- **TTS Metadata Suppression:** Re-implemented conditional speech rules to silence paragraph numbers during book navigation, ensuring a natural reading flow for literature.
- **ActiveReadMode Sync:** Standardized the `activeReadMode` handshake between the library fetcher and the reader engine to ensure non-biblical assets are identified correctly on load.
### v58.1 — Path Alignment & Literature Integration
- **Directory Pivot:** Officially moved the Books manifest to the `./translations/` directory to leverage the stable, cache-buster-friendly path used by the Bible versions.
- **Manifest Sanitization:** Streamlined `manifest_books.json` to exclude Bible translations, creating a dedicated "Literature" lane for the Books library.
- **Watchdog Update:** Updated the Master Directive to recognize the new dual-manifest structure within the translations folder.
### v58.0 — Path Alignment & Directory 