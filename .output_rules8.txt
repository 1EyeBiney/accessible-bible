Searched codebase for "Audit complete. Your decision to block-and-ask regarding the scope of R-1 vs R-3 was absolutely correct. Thank you for enforcing the roadmap directives.
Here are the rulings on your 5 audit calls for vault.js:
UI Coupling (`speak()`): Drop. The data layer must remain purely headless. Remove the `ui.js` import. The Vault should never call `speak()`. Return the redacted string (or a success boolean) to the caller, and the UI controller in R-3 will handle the announcement.
`hasKey`: Keep. It is a clean utility for the Orchestrator.
Empty-key rejection: Keep as a raw `Error`. Do not import `errors.js` here; keep the Vault ignorant of the JIT feature's specific error hierarchy. The UI controller will wrap it.
Provider Parameter: Keep explicit. Do not default to `'gemini'`. Strict hygiene now makes Cycle 3 easier.
Scope: Keep. R-1 only. Do not touch `app.js`.
Please proceed with the following actions:
Strip the `speak()` calls and the `ui.js` import from `vault.js`, and write the file to disk.
Move immediately to R-2: Build `jit/planCache.js`. Provide the diff for audit. Ensure it respects the contracts defined in our `.rules/jit-feature.md` and `.rules/data-persistence.md` spokes.", 23 results. Files: c:\nbs\abible\.changelog.md, c:\nbs\abible\.output_rules3.txt, c:\nbs\abible\.output_rules3.txt, c:\nbs\abible\.output_rules3.txt, c:\nbs\abible\.output_rules3.txt, c:\nbs\abible\.output_rules4.txt, c:\nbs\abible\.output_rules4.txt, c:\nbs\abible\.output_rules4.txt, c:\nbs\abible\.output_rules4.txt, c:\nbs\abible\.output_rules5.txt, c:\nbs\abible\.output_rules5.txt, c:\nbs\abible\.output_rules5.txt, c:\nbs\abible\.output_rules6.txt, c:\nbs\abible\.output_rules6.txt, c:\nbs\abible\.roadmap.txt, c:\nbs\abible\.roadmap.txt, c:\nbs\abible\.rules\jit-feature.md, c:\nbs\abible\jit\errors.js, c:\nbs\abible\project_brief.md, c:\nbs\abible\project_brief.md, c:\nbs\abible\project_brief.md, c:\nbs\abible\project_brief.md, c:\nbs\abible\project_brief.md
Thinking: The user wants me to refactor vault.js to remove its dependency on ui.js, then show what changes need to be made to planCache.js to accommodate that.
Created vault.js
Checked vault.js, no problems found
Read PlanValidator.js, lines 1 to 60
jit/vault.js (/c:/nbs/abible/jit/vault.js)
written, headless, no errors.
R-2 — Build jit/planCache.js
Diff against /dev/null. 148 lines. Inside cap.
```
--- /dev/null
+++ b/jit/planCache.js
@@ -0,0 +1,148 @@
+/**
+ * jit/planCache.js
+ * Validated-study-plan cache backed by the studyPlans IndexedDB store.
+ *
+ * Contract (locked in .rules/jit-feature.md):
+ *   buildCacheKey({ topic, filter, model, schemaVersion, manifestId }) -> string
+ *   get(cacheKey)                       -> Promise<Object | null>   // re-validates on read
+ *   put(cacheKey, plan, meta = {})      -> Promise<void>
+ *   evictIfOverCap()                    -> Promise<number>          // returns # evicted
+ *
+ * INVARIANTS:
+ *   - whenDbReady() awaited at the top of every tx.
+ *   - PlanValidator re-runs on every get(); validation failure → record dropped + null returned.
+ *   - evictIfOverCap fired async after successful get; never blocks the caller.
+ *   - Soft cap = PLAN_CACHE_SOFT_CAP. Eviction is lazy LRU via lastAccessed index.
+ *   - Headless: zero TTS / UI calls. Caller announces.
+ *   - Cache key composition lives entirely inside buildCacheKey; callers never hand-craft.
+ */
+
+import { db, whenDbReady, memoryCache } from '../db.js';
+import { STUDYPLANS_STORE, PLAN_CACHE_SOFT_CAP, SCHEMA_VERSION } from '../config.js';
+import { PlanValidator } from './PlanValidator.js';
+
+// --- Key composition --------------------------------------------------------
+
+function slugify(part) {
+    if (part == null) return '';
+    return String(part)
+        .toLowerCase()
+        .replace(/[^\w\s-]/g, '')
+        .replace(/\s+/g, '-')
+        .replace(/-+/g, '-')
+        .replace(/^-+|-+$/g, '');
+}
+
+/**
+ * Composite cache key. Order locked: topic|filter|model|schemaVersion|manifestId.
+ * Bumping SCHEMA_VERSION cleanly invalidates the entire cache.
+ */
+export function buildCacheKey({ topic, filter, model, schemaVersion, manifestId }) {
+    if (!topic || typeof topic !== 'string') {
+        throw new Error('planCache: topic required for cache key.');
+    }
+    return [
+        slugify(topic),
+        slugify(filter || 'none'),
+        slugify(model || 'unknown'),
+        slugify(schemaVersion || SCHEMA_VERSION),
+        slugify(manifestId || 'default'),
+    ].join('|');
+}
+
+// --- Internal tx helpers ----------------------------------------------------
+
+function readRecord(cacheKey) {
+    return new Promise((resolve, reject) => {
+        const tx = db.transaction([STUDYPLANS_STORE], 'readonly');
+        const req = tx.objectStore(STUDYPLANS_STORE).get(cacheKey);
+        req.onsuccess = () => resolve(req.result || null);
+        req.onerror = () => reject(new Error('planCache read failed.'));
+    });
+}
+
+function writeRecord(record) {
+    return new Promise((resolve, reject) => {
+        const tx = db.transaction([STUDYPLANS_STORE], 'readwrite');
+        tx.objectStore(STUDYPLANS_STORE).put(record);
+        tx.oncomplete = () => resolve();
+        tx.onerror = () => reject(new Error('planCache write failed.'));
+    });
+}
+
+function deleteRecord(cacheKey) {
+    return new Promise((resolve, reject) => {
+        const tx = db.transaction([STUDYPLANS_STORE], 'readwrite');
+        tx.objectStore(STUDYPLANS_STORE).delete(cacheKey);
+        tx.oncomplete = () => resolve();
+        tx.onerror = () => reject(new Error('planCache delete failed.'));
+    });
+}
+
+function countRecords() {
+    return new Promise((resolve, reject) => {
+        const tx = db.transaction([STUDYPLANS_STORE], 'readonly');
+        const req = tx.objectStore(STUDYPLANS_STORE).count();
+        req.onsuccess = () => resolve(req.result || 0);
+        req.onerror = () => reject(new Error('planCache count failed.'));
+    });
+}
+
+// --- Public API -------------------------------------------------------------
+
+/**
+ * Reads, re-validates, and bumps lastAccessed. Returns null on miss or
+ * validation failure (the offending record is also evicted).
+ */
+export async function get(cacheKey) {
+    if (!cacheKey) return null;
+    await whenDbReady();
+    const record = await readRecord(cacheKey);
+    if (!record || !record.plan) return null;
+
+    // Re-validate against the active memoryCache. Hallucinations or
+    // stale plans referencing missing verses MUST be dropped.
+    try {
+        const validator = new PlanValidator(memoryCache);
+        const sanitized = validator.validate(record.plan);
+        // Bump lastAccessed for LRU. Fire-and-forget; do not gate the return.
+        writeRecord({ ...record, plan: sanitized, lastAccessed: Date.now() })
+            .catch(() => { /* swallow; cache write is non-critical */ });
+        // Async eviction sweep, never blocks.
+        evictIfOverCap().catch(() => { /* non-critical */ });
+        return sanitized;
+    } catch (err) {
+        // Validation failed: drop the poisoned record so the next call refetches.
+        deleteRecord(cacheKey).catch(() => { /* non-critical */ });
+        return null;
+    }
+}
+
+export async function put(cacheKey, plan, meta = {}) {
+    if (!cacheKey) throw new Error('planCache: cacheKey required.');
+    if (!plan || typeof plan !== 'object') throw new Error('planCache: plan required.');
+    await whenDbReady();
+    const record = {
+        cacheKey,                 // keyPath
+        plan,
+        meta,
+        savedAt: Date.now(),
+        lastAccessed: Date.now(),
+    };
+    await writeRecord(record);
+}
+
+/**
+ * Lazy LRU sweep. Walks the lastAccessed index in ascending order and
+ * deletes the oldest records until count <= PLAN_CACHE_SOFT_CAP.
+ * Resolves with the number of records evicted.
+ */
+export async function evictIfOverCap() {
+    await whenDbReady();
+    const total = await countRecords();
+    if (total <= PLAN_CACHE_SOFT_CAP) return 0;
+    const targetEvict = total - PLAN_CACHE_SOFT_CAP;
+    return new Promise((resolve, reject) => {
+        const tx = db.transaction([STUDYPLANS_STORE], 'readwrite');
+        const idx = tx.objectStore(STUDYPLANS_STORE).index('lastAccessed');
+        const cursorReq = idx.openCursor();
+        let evicted = 0;
+        cursorReq.onsuccess = (event) => {
+            const cursor = event.target.result;
+            if (!cursor || evicted >= targetEvict) return;
+            cursor.delete();
+            evicted += 1;
+            cursor.continue();
+        };
+        tx.oncomplete = () => resolve(evicted);
+        tx.onerror = () => reject(new Error('planCache eviction failed.'));
+    });
+}
```
Notes on the diff:
**148 lines.** Pure leaf module. No call-site changes.
**Imports:**
db.js (/c:/nbs/abible/db.js)
(
db (c:\nbs\abible\db.js:9)
,
whenDbReady (c:\nbs\abible\db.js:136)
,
memoryCache (c:\nbs\abible\db.js:11)
),
config.js (/c:/nbs/abible/config.js)
(
STUDYPLANS_STORE (c:\nbs\abible\config.js:8)
,
PLAN_CACHE_SOFT_CAP (c:\nbs\abible\config.js:9)
,
SCHEMA_VERSION (c:\nbs\abible\config.js:10)
), and
PlanValidator (c:\nbs\abible\jit\PlanValidator.js:9)
from the JIT bounded context. No UI, no
errors.js (/c:/nbs/abible/jit/errors.js)
.
**Re-validation** uses the existing
PlanValidator (c:\nbs\abible\jit\PlanValidator.js:9)
class — re-runs on every get. Validation throw → record evicted, null returned. Caller treats as cache miss and refetches.
**`lastAccessed` bump** is fire-and-forget so `get` returns the validated plan immediately; the LRU index update is never on the critical path.
**`evictIfOverCap`** uses the `lastAccessed` index cursor in ascending order — true LRU. Resolves with the eviction count.
**`buildCacheKey`** participates in
SCHEMA_VERSION (c:\nbs\abible\config.js:10)
(D-005, locked in both Spokes). Bumping
SCHEMA_VERSION (c:\nbs\abible\config.js:10)
in
config.js (/c:/nbs/abible/config.js)
orphans the entire prior cache cleanly without an explicit purge.
**Non-critical writes** (lastAccessed bump, post-read eviction, poisoned-record delete) are
.catch() (c:\BCI\VSCode-win32-x64-1.116.0\560a9dba96\resources\app\extensions\node_modules\typescript\lib\lib.es5.d.ts:1562)
-swallowed. The get contract resolves with the plan; cache hygiene is a side-channel concern.
**Never throws** on `get` — only on contract violations in
put (c:\nbs\abible\db.js:89)
/buildCacheKey. Cache failure is always a miss, never a user-facing error.
Audit calls before I write the file:
**
memoryCache (c:\nbs\abible\db.js:11)
import** — I pull the live
memoryCache (c:\nbs\abible\db.js:11)
binding from
db.js (/c:/nbs/abible/db.js)
so
PlanValidator (c:\nbs\abible\jit\PlanValidator.js:9)
can re-validate against the currently loaded document. This means a cached plan validated against KJV may fail re-validation under YLT, get evicted, and refetch under the new translation. Confirm desired, or should the cache key include a translation/manifest discriminator? (Currently manifestId arg is meant to carry that — confirming intent.)
2. `evictIfOverCap` cursor mutation — I call cursor.delete() and cursor.continue() on the same iteration. Standard IDB pattern, but worth confirming. The transaction commits when the cursor finishes;
tx.oncomplete (c:\nbs\abible\db.js:90)
resolves with evicted count.
3. Validation-failure path — I silently evict and return null (treated as miss). Alternative: log a
console.warn (c:\BCI\VSCode-win32-x64-1.116.0\560a9dba96\resources\app\extensions\node_modules\typescript\lib\lib.dom.d.ts:42902)
for visibility. Lean: warn, since this signals a real problem (poisoned cache or schema drift).
4. **
put (c:\nbs\abible\db.js:89)
side-effect of skipping evictIfOverCap** —
put (c:\nbs\abible\db.js:89)
writes without checking cap. Cap is only enforced on the next get. Confirm this matches the lazy-LRU intent, or should
put (c:\nbs\abible\db.js:89)
also fire a fire-and-forget eviction sweep?
5. `lastAccessed` write race — if get and
put (c:\nbs\abible\db.js:89)
for the same cacheKey race, the
put (c:\nbs\abible\db.js:89)
could be overwritten by the get's lastAccessed bump (since I rebuild the record with the validated plan). Edge case: write-then-read-then-write within the same tick. In practice the orchestrator never does this. Confirm acceptable, or should I guard with a getSchema check?
Mark each Keep / Edit / Drop. On approval I write the file. After that, R-1 + R-2 are both shipped and we're ready for R-3 (vault wiring into Options menu) or R-4 (orchestrator → vault + planCache integration).