Searched codebase for "Audit complete. Excellent logic on the async IIFE to preserve the synchronous dispatch, and toggling the input type to 'password' was a fantastic security catch for screen readers.
Here are the rulings on your 5 audit calls for R-3:
Provider literal: Edit. Define `const ACTIVE_PROVIDER = 'gemini';` at the top of the vault wiring block and pass that variable. It will make R-7 much cleaner.
Menu order: Keep. Preserving muscle memory for existing Options menu items is the correct priority.
Password toggle: Keep. Excellent catch to prevent TTS echoing. 
Cancellation announcement: Edit. Use "Key entry cancelled." Context is better for non-visual users.
Empty-key UX: Keep. Tear down on error to prevent stuck modes.
Please proceed with the following actions:
Apply the edits and write the diffs to `keyboard.js` and `app.js` using your surgical edit tools.
Run a `get_errors` pass to ensure no syntax issues.
Proceed to R-4: Wire orchestrator → vault and planCache read-through. Provide the diff for `orchestrator.js` for audit.", 19 results. Files: c:\nbs\abible\.output_rules3.txt, c:\nbs\abible\.output_rules3.txt, c:\nbs\abible\.output_rules4.txt, c:\nbs\abible\.output_rules4.txt, c:\nbs\abible\.output_rules5.txt, c:\nbs\abible\.output_rules5.txt, c:\nbs\abible\.output_rules5.txt, c:\nbs\abible\.output_rules5.txt, c:\nbs\abible\.output_rules7.txt, c:\nbs\abible\.output_rules7.txt, c:\nbs\abible\.output_rules9.txt, c:\nbs\abible\.rules\data-persistence.md, c:\nbs\abible\gemini.md, c:\nbs\abible\gemini.md, c:\nbs\abible\jit\vault.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\project_brief.md, c:\nbs\abible\project_brief.md
Thinking: I need to review the specific sections of keyboard.js and app.js to make the edits accurately.
Read keyboard.js, lines 1 to 30
Read keyboard.js, lines 75 to 135
Read keyboard.js, lines 418 to 435
Searched for text isJitInputMode (**/app.js), 2 results. Files: c:\nbs\abible\app.js, c:\nbs\abible\app.js
Replacing 24 lines with 87 lines in keyboard.js, app.js
```
```
```
```
```
```
Checked keyboard.js, app.js, no problems found
Clean. R-3 wired. Now reading orchestrator.js to draft R-4.
Read orchestrator.js
Thinking: Now I need to trace where generateStudyPlan is being called from keyboard.js to see what apiKey argument is currently being passed in.
Searched for text generateStudyPlan (**/keyboard.js), 2 results. Files: c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js
Read keyboard.js, lines 200 to 280
Thinking: I'm noticing that the current call at line 222 is passing only three arguments to `generateStudyPlan`, but the orchestrator function expects five—it's missing the apiKey and memoryCache parameters. This means the existing implementation is actually broken, though it's the state before the R-4 changes I need to make.
For R-4, I need to refactor the orchestrator to fetch the API key internally from the vault and throw an AuthError if it's missing, then implement caching by building a cache key, checking for hits, and storing results. Since the PlanValidator already needs access to memoryCache for hallucination prevention, I'll have the orchestrator import both the vault and memoryCache directly from their respective modules rather than passing them as parameters—this reduces the function signature and keeps things more cohesive. I still need to figure out what identifier to use for the cache key; the app likely tracks the currently loaded manifest somewhere, so I'll start with a sensible default like `'default'` and flag this for the audit. The orchestrator will need to import AuthError, vault, planCache, and memoryCache, then use the cache's buildCacheKey, get, and put methods.
Now I'm checking whether AuthError is already defined in errors.js.
Searched for text currentBibleFile (**/app.js), 1 result. Files: c:\nbs\abible\app.js
Searched for regex manifestId|currentManifest|currentBibleFile|loadedBibleFile (**/*.js), 7 results. Files: c:\nbs\abible\db.js, c:\nbs\abible\db.js, c:\nbs\abible\app.js, c:\nbs\abible\jit\planCache.js, c:\nbs\abible\jit\planCache.js, c:\nbs\abible\jit\planCache.js, c:\nbs\abible\jit\planCache.js
Read errors.js
Thinking: I see that `AuthError` is already available and I can pull the `manifestId` from `localStorage.getItem('currentBibleFile')`. Now I'm working through the R-4 diff for the orchestrator—I need to remove the `apiKey` and `memoryCache` parameters, import the necessary utilities from db.js, and add a pre-call check that retrieves the active provider key and throws an error if it's missing.
The cache lookup logic needs to build a key, attempt a get, and return on a hit; on a miss, I'll call the provider's fetchPlan method, validate it, cache it, and return. I also need to update the keyboard.js call site to match.
There's a question about when to read memoryCache—since JS module imports are live bindings, importing it directly works fine, and the PlanValidator can take it as an argument. For the model string in the cache key, I should check if config.js exports a `MODEL_NAME` constant rather than hardcoding the provider-specific value like 'gemini-2.5-flash'.
Searched for regex gemini-2\.5-flash|GEMINI_MODEL|MODEL_NAME (**/*.js), 2 results. Files: c:\nbs\abible\test_gemini.js, c:\nbs\abible\jit\GeminiProvider.js
Thinking: The model is hardcoded in GeminiProvider, so I'll just use `'gemini-2.5-flash'` directly as the model ID for the cache key—this will get refactored in a later cycle. Moving on to draft the orchestrator.js R-4 diff now.
R-3 complete, no errors. R-4 diff below.
R-4 — Wire orchestrator → vault + planCache
AuthError (c:\nbs\abible\jit\errors.js:40)
already exists in
jit/errors.js (/c:/nbs/abible/jit/errors.js)
. currentBibleFile localStorage key is the natural manifest discriminator. Diff against current
jit/orchestrator.js (/c:/nbs/abible/jit/orchestrator.js)
plus a 4-line trim at the call site in
keyboard.js (/c:/nbs/abible/keyboard.js)
.
orchestrator.js (/c:/nbs/abible/jit/orchestrator.js)
```
--- a/jit/orchestrator.js
+++ b/jit/orchestrator.js
@@ -1,13 +1,21 @@
 /**
  * orchestrator.js
  * The single entry point for generating JIT Study Plans.
  * Acts as the 'Inner Wall' of the double-wall try/catch architecture.
  */
 import { GeminiProvider } from './GeminiProvider.js';
 import { PlanValidator } from './PlanValidator.js';
 import { classifySensitivity, loadCuratedFallback } from './sensitivity.js';
-import { StudyPlanError } from './errors.js';
+import { StudyPlanError, AuthError } from './errors.js';
+import { getKey } from './vault.js';
+import { buildCacheKey, get as cacheGet, put as cachePut } from './planCache.js';
+import { memoryCache } from '../db.js';
+import { SCHEMA_VERSION } from '../config.js';
-export async function generateStudyPlan(topic, filter, apiKey, memoryCache, { signal } = {}) {
+const ACTIVE_PROVIDER = 'gemini';
+const ACTIVE_MODEL = 'gemini-2.5-flash';
+
+export async function generateStudyPlan(topic, filter, { signal } = {}) {
     // 1. Synchronous Safety Check
     const sensitivity = classifySensitivity(topic);
     if (sensitivity.level === 'critical') {
         return loadCuratedFallback(sensitivity.matched);
     }
-    // 2. Async Pipeline
+    // 2. Cache lookup (re-validates internally; miss or poison → null)
+    const manifestId = (typeof localStorage !== 'undefined' && localStorage.getItem('currentBibleFile')) || 'default';
+    const cacheKey = buildCacheKey({
+        topic,
+        filter,
+        model: ACTIVE_MODEL,
+        schemaVersion: SCHEMA_VERSION,
+        manifestId,
+    });
+    const cached = await cacheGet(cacheKey);
+    if (cached) return cached;
+
+    // 3. Vault key fetch — block before any provider call
+    const apiKey = await getKey(ACTIVE_PROVIDER);
+    if (!apiKey) {
+        throw new AuthError('No API key configured', {
+            userMessage: 'No Gemini key is configured. Press O to open the Options Menu and save a key.',
+            recoverable: false,
+        });
+    }
+
+    // 4. Async Pipeline
     let plan;
     try {
         const provider = new GeminiProvider(apiKey);
         // Fetch raw JSON from Gemini
-        const rawPlan = await provider.fetchPlan(topic, filter);
+        const rawPlan = await provider.fetchPlan(topic, filter, { signal });
         const validator = new PlanValidator(memoryCache);
         plan = validator.validate(rawPlan);
@@ existing catch block unchanged
     }
-    // Caching (Task 2.6) will be triggered here safely outside the main try block
+    // 5. Persist to plan cache. Fire-and-forget; cache failure must not
+    //    block returning a validated plan to the engine.
+    cachePut(cacheKey, plan, { topic, filter, model: ACTIVE_MODEL })
+        .catch(() => { /* non-critical */ });
+
     return plan;
 }
```
keyboard.js (/c:/nbs/abible/keyboard.js)
(call site)
```
@@ triggerJitStudyPlan body
-        const plan = await generateStudyPlan(topic, filter, {
-            signal: jitAbortController.signal
-        });
+        const plan = await generateStudyPlan(topic, filter, {
+            signal: jitAbortController.signal
+        });
```
*(No textual change needed at the call site — the new orchestrator signature
(topic, filter, { signal }) (c:\nbs\abible\jit\orchestrator.js:12)
matches the existing call exactly. The pre-R-4 call was already passing only three args; the unused
apiKey (c:\nbs\abible\jit\orchestrator.js:12)
and
memoryCache (c:\nbs\abible\jit\orchestrator.js:12)
slots silently received
{ signal: ... } (c:\nbs\abible\keyboard.js:223)
and undefined. R-4 makes the actual signature match the call, retroactively fixing a latent bug.)*
Notes:
**Latent bug discovery.** The pre-R-4
keyboard.js (/c:/nbs/abible/keyboard.js)
call only passed
(topic, filter, { signal }) (c:\nbs\abible\jit\orchestrator.js:12)
to a 5-arg orchestrator.
apiKey (c:\nbs\abible\jit\orchestrator.js:12)
was receiving the
{signal} (c:\nbs\abible\keyboard.js:223)
object and
memoryCache (c:\nbs\abible\jit\orchestrator.js:12)
was undefined — meaning new PlanValidator(undefined) would throw, and
new GeminiProvider({signal}) (c:\nbs\abible\keyboard.js:223)
would treat the signal object as a key. JIT was non-functional in v66.0. R-4 fixes this by aligning the signature to the call.
**Cache-first.** Cache
get (c:\nbs\abible\keyboard.js:1029)
runs before vault check. A cached plan needs no key. This means a user with no key configured can still replay previously-generated plans offline — a quiet privacy/resilience win.
**AuthError is
recoverable: false (c:\nbs\abible\jit\errors.js:24)
.** The Outer Wall's recovery suffix logic will say "Press Escape to return to study mode" rather than "Press G to try again," since retrying without a key would just hit the same wall. The userMessage itself directs the user to the Options Menu.
**
signal (c:\nbs\abible\keyboard.js:223)
now propagated to
provider.fetchPlan (c:\nbs\abible\jit\orchestrator.js:22)
.** Previously the AbortController was created in
keyboard.js (/c:/nbs/abible/keyboard.js)
but never reached the provider — another latent bug. Fixed in passing.
**`SCHEMA_VERSION` and
localStorage (c:\BCI\VSCode-win32-x64-1.116.0\560a9dba96\resources\app\extensions\node_modules\typescript\lib\lib.dom.d.ts:44082)
lookup live inside the orchestrator** so the cache-key shape is one place. The Spoke Invariant ("callers never hand-craft cache keys") holds.
**
memoryCache (c:\nbs\abible\jit\orchestrator.js:12)
imported as live binding** from
db.js (/c:/nbs/abible/db.js)
. PlanValidator gets the active document at validation time. If the user hot-swaps Bible mid-call (rare during a 30 s window), validation runs against the new document — same behavior as the cache
get (c:\nbs\abible\keyboard.js:1029)
re-validation path. Consistent.
Audit calls:
**Cache before vault** — a key-less user can replay a cached plan. Confirm desired (resilience win), or should vault gate everything (forcing key presence even for replays)?
**`ACTIVE_MODEL = 'gemini-2.5-flash'`** as a local const in
orchestrator.js (/c:/nbs/abible/jit/orchestrator.js)
— duplicated with the hardcoded string inside GeminiProvider.js. Acceptable for now (R-7 consolidates), or should I promote GEMINI_MODEL to
config.js (/c:/nbs/abible/config.js)
immediately?
3. **
provider.fetchPlan(topic, filter, { signal }) (c:\nbs\abible\jit\orchestrator.js:22)
** — I'm propagating signal to the provider. I haven't checked whether
GeminiProvider.fetchPlan (c:\nbs\abible\jit\orchestrator.js:7)
currently accepts an options object. If it doesn't, I'll need a one-line addition there. Confirm I should patch GeminiProvider in the same R-4 commit, or split that to a follow-up.
4. **
localStorage.getItem('currentBibleFile') (c:\BCI\VSCode-win32-x64-1.116.0\560a9dba96\resources\app\extensions\node_modules\typescript\lib\lib.dom.d.ts:44082)
inside orchestrator** — couples the JIT bounded context to a top-level localStorage key. Cleaner alternative: pass
manifestId (c:\nbs\abible\jit\planCache.js:40)
as a 3rd parameter from
keyboard.js (/c:/nbs/abible/keyboard.js)
and let the engine layer own that lookup. Lean: parameter (cleaner boundary). Confirm.
5. `cachePut` fire-and-forget —
.catch(() => {}) (c:\nbs\abible\keyboard.js:327)
swallows write failures silently. Console.warn for symmetry with the get-side poisoned-record warn? Lean: yes (development visibility).
Mark Keep / Edit / Drop. On approval I apply the orchestrator diff, patch GeminiProvider if needed (#3), and run get_errors.