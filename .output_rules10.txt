Searched codebase for "Approve. The Save State diff is perfectly executed and respects all formatting and token-economy constraints.
Regarding the blocker (OQ-7): We will resolve this with immediate overwrite. Because Invariant j forbids native browser modals, building a custom in-DOM confirmation state just for this action adds unnecessary complexity to the UI layer. An immediate overwrite paired with a clear TTS announcement (handled by the UI controller) is the most efficient and accessible path.
Please proceed with the following actions:
Apply the Save State diff to `project_brief.md`.
Begin R-3: Wire vault into Options menu UX. Please provide the diff for the UI wiring. Remember to respect the B1 idempotent listener pattern, the `ui.js` separation of concerns, and our `activeInputHandler` routing.", 24 results. Files: c:\nbs\abible\.output_rules1.txt, c:\nbs\abible\.output_rules1.txt, c:\nbs\abible\.output_rules2.txt, c:\nbs\abible\.output_rules3.txt, c:\nbs\abible\.output_rules3.txt, c:\nbs\abible\.output_rules3.txt, c:\nbs\abible\.output_rules3.txt, c:\nbs\abible\.output_rules4.txt, c:\nbs\abible\.output_rules4.txt, c:\nbs\abible\.output_rules4.txt, c:\nbs\abible\.output_rules4.txt, c:\nbs\abible\.output_rules5.txt, c:\nbs\abible\.output_rules6.txt, c:\nbs\abible\.output_rules6.txt, c:\nbs\abible\.output_rules7.txt, c:\nbs\abible\.output_rules7.txt, c:\nbs\abible\.rules\jit-feature.md, c:\nbs\abible\jit\vault.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\project_brief.md, c:\nbs\abible\project_brief.md, c:\nbs\abible\project_brief.md, c:\nbs\abible\project_brief.md, c:\nbs\abible\project_brief.md
Thinking: I'm going to apply the save state diff first, then work through planning for R-3.
Replacing 16 lines with 17 lines in project_brief.md
```
```
```
```
Save State applied. Brief is now v1.1.
Now for R-3 I need to inspect the actual Options menu code path so the diff is grounded in what exists. Block-and-ask before drafting.
Searched for regex Options|isOptionsMode|optionsMenu (**/*.js), 20 results. Files: c:\nbs\abible\app.js, c:\nbs\abible\app.js, c:\nbs\abible\app.js, c:\nbs\abible\app.js, c:\nbs\abible\app.js, c:\nbs\abible\app.js, c:\nbs\abible\app.js, c:\nbs\abible\config.js, c:\nbs\abible\config.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js
Searched for regex searchInputEl|activeInputHandler (**/keyboard.js), 20 results. Files: c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js
Searched for text isOptionsMenuMode (**/keyboard.js), 13 results. Files: c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js
Read keyboard.js, lines 770 to 895
Thinking: I need to locate where the Options menu gets triggered and what menu items are currently displayed, so I can figure out the right place to insert the new Save Key, Replace Key, and Clear Key options.
Read keyboard.js, lines 1240 to 1290
Read keyboard.js, lines 1440 to 1490
Read keyboard.js, lines 1255 to 1280
Thinking: I'm searching for where the `O` key handler is implemented — the one that opens the Options menu for importing and exporting data.
Searched for text Export Personal Notes (**/keyboard.js), 3 results. Files: c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js, c:\nbs\abible\keyboard.js
Read keyboard.js, lines 1342 to 1360
Thinking: I see where the Options Menu gets triggered by pressing O, and it currently has three items. The Enter key handler is in the `isOptionsMenuMode` block around line 825-895. For the R-3 requirement, I need to add three new menu entries: one that either says "Save Gemini Key" or "Replace Gemini Key" depending on whether a key already exists (with a redacted display), and a separate "Clear Gemini Key" option.
The architecture needs to follow several constraints: the keyboard listener must be idempotent with a single `activeInputHandler` slot that gets cleaned up in `clearAllModes`, UI announcements go through `speak()` in ui.js with no DOM manipulation in keyboard.js, vault.js stays headless and returns the redacted string after `setKey` is called, no browser modals are allowed, and I need a new `isVaultInputMode` flag for the key-entry phase that gets added to the focus-trap blur exclusion list.
The flow works like this: when the user presses O, the Options Menu opens and I need to await both `redactedDisplay('gemini')` and `hasKey('gemini')` to populate the dynamic label before rendering the menu. When they select Save or Replace Key, I clear all modes, set `isVaultInputMode`, focus the search input, and attach the handler—on Enter it calls `setKey` and announces the success with the redacted ending, or on Escape it cancels. Selecting Clear Key just calls `clearKey` and announces the result.
Since the Options Menu is shared across multiple menu types, I'll branch on the selected text rather than checking the menu title. The vault input handler follows the same pattern as the existing `jitInputHandler`. I need to touch keyboard.js for the main wiring and handler logic, app.js for the focus-trap exclusion, and ui.js doesn't need changes since `speak()` handles announcements. Before drafting the diff, I should confirm the provider is hardcoded as `'gemini'` since multi-provider support isn't until R-7, decide whether to append the Save/Replace/Clear options at the end of the menu or insert them elsewhere, and clarify how the menu count announcement should work when keys are present.
The menu item count changes dynamically depending on whether a key exists—5 items with a key, 4 without—so the opening announcement needs to reflect that. The `O` handler is currently synchronous but vault operations are async, so I'm leaning toward wrapping the case in an async IIFE to keep the change minimal. The focus-trap exclusion in app.js needs `isVaultInputMode` added alongside the existing `isJitInputMode` check. For the TTS announcement when a key is saved, I'll strip the bullet prefix from the vault's redacted display and announce just the last four characters.
When an empty key is submitted, vault.js rejects it with an error that the Options menu handler catches and announces—the question is whether to close the input mode afterward or keep it open for the user to retry. I'm leaning toward announcing and closing, letting them re-enter through the menu if needed.
I have enough context. Before the diff: this work spans
keyboard.js (/c:/nbs/abible/keyboard.js)
(main) and one line in
app.js (/c:/nbs/abible/app.js)
(focus-trap blur exclusion for the new input mode).
ui.js (/c:/nbs/abible/ui.js)
is untouched —
speak() (c:\nbs\abible\keyboard.js:1)
covers all announcements. Spoke jit-feature §INVARIANTS hold: B1 listener pattern reused exactly, vault stays headless, no browser modals.
Here is the diff for audit. ~95 lines across two files.
keyboard.js (/c:/nbs/abible/keyboard.js)
```
@@ imports
 import {
     searchInputEl, noteEditorEl, importFileEl, importCommentaryEl, tutorialAudioEl,
@@ (existing imports continue)
 } from './ui.js';
+import { getKey, setKey, clearKey, hasKey, redactedDisplay } from './jit/vault.js';
@@ // Mode flags around line 82
 export let isOptionsMenuMode = false;
+export let isVaultInputMode = false;   // active while user types API key into searchInputEl
 export let isAutoPlayMenuMode = false;
@@ inside clearAllModes() — line ~127
-    isSearchMode = false; isNoteMode = false; isOptionsMenuMode = false; isAutoPlayMenuMode = false;
+    isSearchMode = false; isNoteMode = false; isOptionsMenuMode = false; isAutoPlayMenuMode = false;
+    isVaultInputMode = false;
@@ menu-mode union — line ~426
                        (typeof isOptionsMenuMode !== 'undefined' && isOptionsMenuMode) ||
+                       (typeof isVaultInputMode !== 'undefined' && isVaultInputMode) ||
@@ Options Menu Enter dispatch — append after the 'Import Personal Notes' branch (~line 862)
             if (selected === 'Import Personal Notes') { importFileEl.click(); isOptionsMenuMode = false; return; }
+            // --- BYOK Vault entries -------------------------------------------------
+            if (selected === 'Save Gemini Key' || selected.startsWith('Replace Gemini Key')) {
+                clearAllModes();
+                isVaultInputMode = true;
+                if (!searchInputEl) { speak("Input unavailable."); return; }
+                searchInputEl.value = '';
+                searchInputEl.placeholder = 'Paste Gemini API key, then Enter';
+                searchInputEl.type = 'password';
+                speak("Enter Gemini API key. Paste then press Enter, or Escape to cancel.");
+                setTimeout(() => searchInputEl.focus(), 10);
+
+                const vaultInputHandler = async (e) => {
+                    if (e.key !== 'Enter' && e.key !== 'Escape') return;
+                    e.preventDefault();
+                    const raw = searchInputEl.value || '';
+                    searchInputEl.value = '';
+                    searchInputEl.placeholder = '';
+                    searchInputEl.type = 'text';
+                    searchInputEl.removeEventListener('keydown', vaultInputHandler);
+                    activeInputHandler = null;
+                    isVaultInputMode = false;
+                    if (e.key === 'Escape') { speak("Cancelled."); return; }
+                    try {
+                        const redacted = await setKey('gemini', raw);
+                        const last4 = redacted.replace(/^•+\s*/, '');
+                        speak(`Gemini key saved, ending in ${last4}.`);
+                    } catch (err) {
+                        speak("Empty key rejected. Please try again from the Options Menu.");
+                    }
+                };
+                activeInputHandler = vaultInputHandler;
+                searchInputEl.addEventListener('keydown', vaultInputHandler);
+                return;
+            }
+
+            if (selected === 'Clear Gemini Key') {
+                isOptionsMenuMode = false;
+                clearKey('gemini')
+                    .then(() => speak("Gemini key cleared."))
+                    .catch(() => speak("Could not clear key."));
+                return;
+            }
+            // --- end BYOK Vault entries ---------------------------------------------
+
             // Omni-Jump Selection Link Handling
             if (/^\[\[.*\]\]$/.test(selected)) {
@@ case 'O' — line ~1343
         case 'O':
             event.preventDefault();
             if (!isReady) break;
             clearAllModes();
-            isOptionsMenuMode = true;
-            menuOptions = ['Export Personal Notes', 'Import Personal Notes', 'Boot Location: ' + bootPreference];
-            currentMenuIndex = 0;
-            currentMenuTitle = "OPTIONS MENU";
-            renderMenuVisuals(currentMenuTitle, menuOptions, currentMenuIndex);
-            speak("Options Menu. 1 of 3: Export Personal Notes. Up and down arrows to navigate, Enter to select, Escape to close.");
+            (async () => {
+                const keyPresent = await hasKey('gemini');
+                const baseOpts = ['Export Personal Notes', 'Import Personal Notes', 'Boot Location: ' + bootPreference];
+                if (keyPresent) {
+                    const redacted = await redactedDisplay('gemini');
+                    baseOpts.push(`Replace Gemini Key (${redacted})`);
+                    baseOpts.push('Clear Gemini Key');
+                } else {
+                    baseOpts.push('Save Gemini Key');
+                }
+                isOptionsMenuMode = true;
+                menuOptions = baseOpts;
+                currentMenuIndex = 0;
+                currentMenuTitle = "OPTIONS MENU";
+                renderMenuVisuals(currentMenuTitle, menuOptions, currentMenuIndex);
+                speak(`Options Menu. 1 of ${menuOptions.length}: Export Personal Notes. Up and down arrows to navigate, Enter to select, Escape to close.`);
+            })();
             break;
```
app.js (/c:/nbs/abible/app.js)
```
@@ keyboard.js mode-flag import (existing)
-import { ..., isJitInputMode, isJitLoading, ... } from './keyboard.js';
+import { ..., isJitInputMode, isJitLoading, isVaultInputMode, ... } from './keyboard.js';
@@ focus-trap blur exclusion (existing line that already lists JIT flags)
-        if (!isSearchMode && !isNoteMode && !isOptionsMenuMode && !isJitInputMode && !isJitLoading /* etc */) {
+        if (!isSearchMode && !isNoteMode && !isOptionsMenuMode && !isJitInputMode && !isJitLoading && !isVaultInputMode /* etc */) {
             focusTrap.focus();
         }
```
Notes:
**Headless contract preserved.** `vault.js` returns `"•••• 7g4Q"`; the
keyboard.js (/c:/nbs/abible/keyboard.js)
handler strips the bullets and routes a TTS-safe string through
speak() (c:\nbs\abible\keyboard.js:1)
. UI announcement lives in the controller, not the data layer.
**B1 idempotent listener pattern reused exactly.** `vaultInputHandler` is assigned to
activeInputHandler (c:\nbs\abible\keyboard.js:116)
;
clearAllModes() (c:\nbs\abible\keyboard.js:118)
will detach it on any new mode entry; the handler self-detaches on Enter/Escape and nulls
activeInputHandler (c:\nbs\abible\keyboard.js:116)
.
**No browser modals (Invariant j).** Immediate overwrite per OQ-7 ruling — `setKey` simply replaces; the announcement "Gemini key saved, ending in 7g4Q" is the user's confirmation.
**Mode union (line ~426)** is the existing condition that prevents global keystrokes during input modes. Adding `isVaultInputMode` to the union keeps Outer Wall semantics intact.
**Async `O` handler** uses an IIFE so the `case` statement stays sync at the dispatch level.
clearAllModes() (c:\nbs\abible\keyboard.js:118)
runs synchronously before the IIFE so any pending mode is torn down deterministically.
**
searchInputEl.type = 'password' (c:\nbs\abible\app.js:35)
** during input prevents the key from being read aloud by sighted-mode TTS (browser Speech rate-limits announce on input fields). Reverts to
'text' (c:\nbs\abible\jit\PlanValidator.js:24)
on exit.
**Empty-key path** catches the raw
Error (c:\BCI\VSCode-win32-x64-1.116.0\560a9dba96\resources\app\extensions\node_modules\typescript\lib\lib.es2022.error.d.ts:23)
from vault and announces a recovery instruction. Mode is already torn down by the time we hit the catch.
Audit calls:
**Provider literal `'gemini'`** — hardcoded everywhere in this diff. R-7 (multi-provider) will introduce a selector. Acceptable today, or do you want a single `const PROVIDER = 'gemini'` at file top now?
**Menu order** — I appended the vault entries *after* "Boot Location". Alternative: insert before "Export Personal Notes" so the highest-frequency new feature is at position 1. Lean: keep at end to preserve existing muscle memory. Confirm.
**
searchInputEl.type = 'password' (c:\nbs\abible\app.js:35)
** — toggles type mid-session. Adds a temporary attribute mutation we revert on exit. Confirm acceptable, or should I leave the input as plain text and rely on the user not to read it aloud?
4. Cancellation announcement — Escape during key entry says &quot;Cancelled.&quot; Sparse on purpose (no key reference). Confirm wording, or prefer &quot;Key entry cancelled.&quot;?
5. Empty-key UX — current diff tears down the input mode and tells the user to retry from the Options Menu. Alternative: stay in input mode, beep, await another paste. Lean: tear down (cleaner state, prevents stuck modes). Confirm.
Mark Keep / Edit / Drop. On approval I apply both file edits and run a get_errors validation pass.