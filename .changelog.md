
### v0.30.1 — The Modular Architecture Update
- **Architecture:** Transitioned from a monolithic `app.js` to an ES6 Module system to improve code maintainability and screen reader navigation speed.
- **`config.js`:** Extracted all static constants, theme definitions, and audio playlists into a dedicated configuration module.
- **`ui.js`:** Centralized the ARIA announcement logic (`speak`) to ensure a single, consistent "voice" across all engine components.
- **`audio.js`:** Isolated the Web Audio API synthesizer, ambient crossfader, and Audio Codex logic into a self-contained sound module.
- **`db.js`:** Modularized the IndexedDB pipeline, including library fetching, memory caching, and bookmark persistence.
- **Developer UX:** Implemented an "Accessible Error Catcher" in `index.html` to announce syntax and loading errors via TTS, bypassing the inaccessible browser console.
### v0.29.0 — Developer Environment Reset
- **Developer Hotkey (`app.js`):** Added a hidden `Shift + R` command to instantly clear `localStorage` and trigger a `location.reload()`, allowing rapid testing of conditional boot sequences without requiring the browser's developer console.
### v0.28.0 — Audio Codex Foundation
- **Interface (`index.html`):** Built `#tutorial-screen` overlay for sighted users with a high-contrast "Now Playing" layout and media control legend.
- **Boot Sequence (`app.js`):** Added `muteTutorialPrompt` (persisted to `localStorage`). Updated `loadToMemory()` and `endWelcomeSequence()` to append the Audio Codex instructions dynamically based on user preference.
- **Media Player Logic (`app.js`):** Implemented `isTutorialMode`. Mapped `H` to launch the Codex and `Shift + H` to toggle the boot prompt. Added dedicated keyboard hooks inside Tutorial Mode for Spacebar (Play/Pause), Left/Right (Seek 10s), Up/Down (Skip Chapters), and Escape (Exit).
c## v0.27.3 — Airlock Focus Trap & Ambient Audio Shift
- **Focus Preservation (`index.html`, `app.js`):** Added `role="application"` and `tabindex="0"` to `#welcome-screen`, and applied `.focus()` on load to prevent screen readers (NVDA/JAWS) from dropping into Browse Mode, ensuring the `Right Arrow` key is passed to the engine.
- **Ambient Audio Shift (`app.js`):** Modified `playNextTrack(suppressTTS = false)` and invoked it inside `startWelcomeSequence()` to begin ambient music immediately on the orientation screen without TTS collision.
### v0.27.2 — Airlock Path & ARIA Patch
- **Audio Path (`index.html`):** Updated the `welcome-audio` source to point to `./audio/dialog/welcome.mp3`.
- **Screen Reader Silence (`index.html`):** Applied `aria-hidden="true"` to the Welcome Screen text container to prevent TTS from automatically reading the visual orientation script, ensuring only the stealth bypass and recorded audio are heard.
### v0.27.1 — Welcome Screen Airlock (Static Text) & History Restoration
- **Logic Implementation (`app.js`):** Injected the `startWelcomeSequence()`, `endWelcomeSequence()`, and `handleInput()` keyboard traps to successfully enforce the keyboard-only orientation screen. Updated `activateEngine()` to respect the `skipWelcome` local storage flag.
- **Documentation (`gemini.md`):** Restored the changelog history for v0.25.0 (Visual Headers) and v0.26.0 (Ambient Audio) that were previously erased during a partial AI completion.
## v0.27.0 - The "Airlock" Welcome Screen
- **Onboarding Interface (`index.html`, `app.js`):** Implemented a mandatory Welcome Screen phase that executes immediately after the splash screen. This features a visual typewriter effect, user-select prevention (to discourage mouse use), and an accompanying audio tutorial (`welcome.mp3`).
- **Input Trapping & Bypass:** Created `isWelcomeMode` to intercept and block all navigation keys until the user acknowledges the keyboard-only philosophy via the `Right Arrow` key. Screen reader users are provided a stealth TTS prompt to press `Escape` to bypass the tutorial. Users can also press `X` to permanently save a `skipWelcome` flag to local storage.
## v0.26.0 - Ambient Audio Expansion
- **Hymn Library (`app.js`):** Expanded the `hymnList` array with 16 new ambient tracks, including variations of A Mighty Fortress, Blessed Assurance, Crown Him With Many Crowns, Great Is Thy Faithfulness, Rock of Ages, and What a Friend We Have in Jesus. The `grabBag` shuffle logic automatically incorporates these into the crossfade rotation.

##                      v0.25 Added Visuals
- **Visual Reference Injection (`app.js`):** Added dynamic DOM injection to display the current book, chapter, and verse in standard notation (e.g., "Romans 1:1") at the top of the `#content-display` container. This provides persistent, scalable situational awareness for low-vision users without interfering with the screen reader's optimized audio prefix.
## v0.24.0 - Low-Vision Hybrid Interface & Theme Engine
- **Dynamic Scaling (`index.html`, `app.js`):** Implemented `-` and `=` hotkeys to dynamically adjust the `--base-font-size` CSS variable without triggering browser-level layout zooming.
- **Theme Carousel:** Added the `T` key command to cycle through specialized high-contrast color profiles (Midnight, Amber Terminal, Macular Yellow, Cyan Wash).
- **Stealth Visual HUD:** Added a persistent bottom command strip and dynamic verse alert pills (Note/Commentary). Both are wrapped in `aria-hidden="true"` to provide massive visual targets for low-vision users while remaining completely invisible to screen readers.
## v0.23.0 - Relational Commentary Audio Patch
- **Commentary Audio Cue (`app.js`):** Integrated the "Radiation Ping" (Sound 91) from the Bunker Laboratory. This high-frequency (1800Hz) synthesized cue triggers automatically when landng on a verse containing expert commentary.
- **Staggered Navigation UI:** Updated `readCurrentVerse` to perform parallel lookups. Added a 150ms delay between personal note indicators and expert commentary pings to ensure audio clarity for screen reader users.
## 0.22.0 - The Omni-Parser Upgrade
### Changed
* Link Resolution: Upgraded `parseLinkTarget()` with a comprehensive `bookNameMap`. The engine now automatically translates biblical abbreviations (e.g., "Prov", "Gen", "1 Cor") into full book names, ensuring compatibility with external datasets like the TSK.
* Versioning: Synchronized project headers to v0.22.0.
## 0.21.0 - Curriculum Sync Architecture
### Fixed
* Commentary Mapping: Fixed a silent failure where the engine's internal string-based `id` failed to match the numeric integers generated by the TSK Node.js script. 
### Changed
* ID Generation: Implemented a dynamic `curriculumId` generator (`(book_number * 1000000) + (chapter * 1000) + verse`) to serve as a universal bridge between varying Bible datasets and external commentary files.
* Expert Readout (`Shift + Up Arrow`): Upgraded to query `COMMENTARY_STORE` using the dynamic `curriculumId` rather than the native string ID.
* Omni-Jump (`Alt + J`): Upgraded the relational engine to query `NOTES_STORE` using the native ID, and `COMMENTARY_STORE` using the dynamic `curriculumId`, perfectly syncing both datasets.
* Versioning: Synchronized project headers to v0.21.0.
## 0.20.1 - Curriculum Architecture (Hotfix)
### Fixed
* Injection Error: Patched missing logic from the v0.20.0 update that was dropped during file generation.
* Options Menu: Officially wired the `O` key logic and its execution block inside `isMenuMode`.
* Expert Readout: Officially wired the `Shift + Up Arrow` toggle.
* Omni-Jump: Officially replaced the `Alt + J` block with the dual-store scanning logic.
* Commentary Importer: Added the `FileReader` listener for the `import-commentary-file` input element.
### Changed
* Versioning: Synchronized project headers to v0.20.1.
## 0.20.0 - The Curriculum Architecture
### Added
* Database: Bumped `DB_VERSION` to 6. Added `COMMENTARY_STORE` to house expert notes and guided reading tracks.
* Options Menu: Added an administrative hub bound to the `O` key. Consolidates "Export Notes", "Import Notes", "Import Commentary", and "Clear Commentary".
* Expert Readout: Mapped `Shift + Up Arrow` to instantly announce the commentary for the current verse.
### Changed
* Omni-Jump (`Alt + J`): Upgraded the relational link scanner to extract `[[links]]` from BOTH `NOTES_STORE` and `COMMENTARY_STORE` simultaneously, merging them into a single deterministic menu.
* Link Parsing: Upgraded `parseLinkTarget()` regex to safely handle multi-verse ranges and comma-separated lists (e.g., `[[Prov 8:22-24]]`), jumping to the base verse.
* Keybindings: Deprecated `Shift + E` and `Shift + I` to reduce hotkey bloat.
## 0.19.1 - Bookmarking System Logic (Hotfix)
### Fixed
* Bookmark Engine: Injected the missing javascript logic into `app.js` for the v0.19.0 bookmarking update.
* Initialization: Added `loadBookmarks()` to pull the `BOOKMARKS_STORE` array into `bookmarksCache` on boot.
* Keybindings: Activated the `K` key toggle (with the 10-slot limit) and the `Shift + [` / `Shift + ]` canonical carousel navigation.
### Changed
* Versioning: Synchronized project headers to v0.19.1.
## 0.19.0 - Bookmarking System
### Added
* Bookmarks Store: Bumped `DB_VERSION` to 5 and added a dedicated `BOOKMARKS_STORE` to IndexedDB.
* Bookmark Toggle (`K`): Implemented a single-key toggle to add or remove the current verse from the bookmarks cache. Includes a hard limit of 10 active bookmarks to preserve navigation efficiency.
* Bookmark Carousel (`Shift + [` and `Shift + ]`): Implemented sequential navigation through saved bookmarks. The engine automatically sorts bookmarks in canonical Bible order for logical reading flow.
## 0.18.2 - Help Menu & Explorer Mappings (Hotfix)
### Added
* Help Menu Dialog: Implemented the `isHelpMode` intercept and `helpMenuData` array. Pressing `?` now opens a vertical, scrollable list of all engine commands.
* Keyboard Explorer Dictionary: Populated the `getKeyboardExplorerDescription` function with the actual engine mappings so it announces the function of the key, rather than just repeating the key name.
## 0.18.1 - Keyboard Explorer Toggle (Hotfix)
### Changed
* Keyboard Explorer Hotkey: Remapped the Keyboard Explorer activation from `K` to `F12`.
* Toggle Behavior: The Keyboard Explorer is now a true toggle. Pressing `F12` while it is active will turn it off, alongside the existing `Escape` key exit method. 
* Versioning: Bumped project headers to v0.18.1.
## 0.17.1 - TTS Conflict Resolution (Hotfix)
### Fixed
* Announcement Overlap: Removed the hardcoded `speak("Crossfading to next track.")` from the `KeyN` input handler. This prevents the screen reader from immediately interrupting the dynamic `formatSongTitle` announcement triggered by the `playNextTrack()` engine.
### Changed
* Versioning: Bumped project headers to v0.17.1 to reflect the hotfix.

## 0.17.0 - Ambient Song Announcements
### Added
* Natural Title Parser: Implemented `formatSongTitle` to sanitize filenames (removing extensions, trailing version digits, and underscores) for natural TTS readout.
* "Now Playing" Feedback: The system now automatically announces the title of each hymn as it begins to play, including when manually skipped.
### Changed
* Navigation Logic: Refactored the 'N' key case to use the unified announcement logic within the track crossfade engine.
* Versioning: Incremented project version to v0.17.0 across all master files.

### Added
* Natural Title Parser: Implemented `formatSongTitle` to sanitize filenames (removing extensions, trailing version digits, and underscores) for natural TTS readout.
* "Now Playing" Feedback: The system now automatically announces the title of each hymn as it begins to play, including when manually skipped.
### Changed
* Navigation Logic: Refactored the 'N' key case to use the unified announcement logic within the track crossfade engine.
* Versioning: Incremented project version to v0.17.0 across all master files.Number.
## 0.16.0 - Deployment Pathing & Architecture Finalization
### Fixed
* Audio Asset Routing: Transitioned from absolute (`/audio/`) to relative (`./audio/`) pathing for the ambient music engine. This ensures assets load correctly on both local development servers and GitHub Pages subdirectories.
### Changed
* Jump Command: Confirmed the remapping of the Relational Jump command to `Alt + J` to prevent browser conflicts with Chrome's native AI shortcuts.
* Documentation: Synchronized the Master Watchdog Directive with all v0.15.0 logic, including the breadcrumb navigation history and the "Anchor and Drop" relational workflow.
## 0.15.0 - Hotkey Remapping (Browser Conflict Resolution)
### Changed
* Jump Hotkey: Remapped the Relational Jump command from `Alt + G` to `Alt + J` to prevent conflicts with Chrome's native AI menus.
* Engine Update: Updated the global `handleInput` switch case and the local `#note-editor` keydown listener to target `KeyJ`.
* Documentation: Updated Section 3 and Section 6 of `gemini.md` to reflect the `Alt + J` workflow.
## 0.14.0 - Hybrid Relational Engine, Verse Menus, & Local Backups
### Added
* Breadcrumb Stack: Added a `navigationHistory` array. Teleporting pushes the current index; pressing `Backspace` pops the history to retrace steps.
* Anchor & Link System: 
    * `R` anchors a verse coordinate in memory.
    * `Alt + L` (Global) silently appends the anchored link to the current verse's note.
    * `Alt + L` (Local) injects the anchored link at the cursor in the Note Editor.
    * `Alt + G` (Global) scans the current note for `[[links]]` and auto-teleports (or opens a menu if multiple are found).
    * `Alt + G` (Local) teleports to the link currently under the text editor cursor.
* Vertical Axis Actions: 
    * `Up Arrow` instantly reads the current verse's note from the database (if it exists).
    * `Down Arrow` opens the temporary Verse Action Menu (`isMenuMode`).
* Verse Action Menu: Includes Options to Edit Note, Delete Note, and Copy Verse to system clipboard.
* Global Data Backups: `Shift + E` exports the `NOTES_STORE` as a JSON file. `Shift + I` imports and merges a JSON backup into the database via a stealth file input.
## 0.13.0 - Synthesized Audio Engine & Note Indicators
### Added
* Web Audio API Integration: Ported the `AudioContext`, `playTone`, and `playSequence` synthesizer architecture from Accessible Golf.
* Audio Gain Control: Implemented the `AUDIO_GAIN_BOOST` constant to ensure synthesized alerts cut through the mix.
* Note Indicator: Navigating to a verse that contains a saved note now triggers an instant, zero-latency ascending synthesized sequence (Sound 31: 1000Hz, 1500Hz, 2000Hz, 2500Hz sines).
### Changed
* Database Check Integration: `readCurrentVerse()` now performs an asynchronous, read-only IndexedDB lookup on the `NOTES_STORE` every time a verse is loaded to determine if the audio indicator should fire.
* Initialization: Added `initAudio()` to the `activateEngine` boot sequence to satisfy browser audio-context interaction requirements.
## 0.12.0 - Digital Margins & Cache-Busting Pipeline
### Added
* Memo Mission (Digital Margins): Injected a stealth `<textarea>` (`#note-editor`) mapped to the `M` key. Users can now read, write, and save personal notes bound to the absolute ID of the current verse.
* Note Persistence: Notes are saved to the IndexedDB `userNotes` store, ensuring they persist across sessions and database upgrades.
* Cache-Busting Pipeline: Upgraded the `initDatabase` logic so that incrementing the `DB_VERSION` automatically bulldozes the old text cache (while protecting user notes) and forces a fresh network fetch.
* Global Escape: Added a global `Escape` intercept to standard navigation that silently clears all active searches and modes.
### Changed
* Data Source: Updated the `fetch()` target to `bsb1.json`.
* DB Version: Incremented `DB_VERSION` to `4` to trigger the new bulldozer upgrade logic.
* `gemini.md`: Updated Section 3 (Keybindings) for the `M` and `Escape` keys. Added Section 5 (Data Pipeline) documenting the cache-busting strategy.
## v0.11.2 Another cleaning pass
##v0.11.0 Cleaned up bsb.jsonRight. That's all he gets. OK. So you're saying I'm website? We should? I. OK. Yes. Hey, Cortana, if I just paid 500? Bryant asked. How about he just paid 500? Yeah. Text OK, yeah. Just a cover. And we'll get ahead of it. He said to cover it and be ahead of it. OK. You said 500 again? OK. OK and I will get I will look on the government. Yeah, yeah. OK sounds good. Yes. Right. Thanks. Absolutely. Hi, Daddy. How's the how's the kids? No, this is the first year I did not do it. I just. I had some other stuff going on, I just didn't have time to sit down and do it. You know, this is a lot of the three games this year, up and down season, but overall it was a pretty weird. OK well sure, he he was sure fun and what a great career and. Yeah, just hard and hustle and all that kind of stuff. Oh yeah, it got a big guy coming from Princeton and they got born. Keep coming in. Who knows? We'll see how he doesn't. Be sad to see those guys go for so long. Play Darby. Mr Dee from 95,246. 715099 messages. 715. 099. Hey, Cortana. Jason. Like. No. Like like like like. Gemini reply. I. slash one slash J slash
##0.11.0 - Zero-Latency Search Mission
### Added
* Stealth Search Input: Injected a visually hidden `<input type="text">` (`#search-input`) to capture search queries natively without breaking the Focus Trap architecture.
* Instant Memory Filtering: Built a high-speed search function that filters the `memoryCache` and populates a state-based `searchResults` array.
* Search Carousel: Mapped `[` (Previous) and `]` (Next) keys to cycle rapidly through active search results. 
* Contextual Drop-in: Viewing a search result automatically updates the main index, allowing immediate context reading via the Left/Right arrows.
* Search Status: Updated the `S` key to append active search statistics (e.g., "Search active: viewing match 4 of 42") if a search is loaded.
### Changed
* Mode Safety: `clearAllModes()` now safely dumps the `searchResults` array when navigating away via coordinate jumps (`B`, `C`, `V`, or Page keys).
* `gemini.md`: Updated Section 3 (Keybindings) to include `F`, `[`, and `]`. Updated Section 4 to mandate the Stealth Input Protocol for searches.
## 0.10.0 - Ambient Audio Engine and Grab Bag Playlist
### Added
* Ambient Audio Engine: Implemented a dual-buffer audio system to support seamless crossfading between background tracks.
* Grab Bag Playlist: Added a shuffle-and-pop algorithm for the 10 hymn assets to ensure non-repeating playback.
* Auto-Play Initialization: The ambient music now begins playing automatically at volume level 10 once the application is initialized.
* Volume Control: Mapped `Shift + V` to cycle through ambient volume stages (0, 5, 10, 20, 30, 40).
* Next Track: Mapped the `N` key to manually trigger a crossfade to the next random track.
### Changed
* Key Re-mapping: Moved the "Digital Margins" note-taking mission from `N` to `M` (Memo) to accommodate the new audio controls.
* `gemini.md`: Updated Section 3 (Keybindings) to reflect the new `M`, `N`, and `Shift + V` assignments.